# BGW Website Rebuild

Password: BGW

Upload all files and folders to your GitHub repository, replacing the old site files. GitHub Pages will publish automatically.

For true private access and easier editing, move the repo to Netlify and enable Identity + Git Gateway. The included /admin folder is a starter Decap CMS setup, but the current site is static and should be treated as code-first until CMS fields are fully mapped.
